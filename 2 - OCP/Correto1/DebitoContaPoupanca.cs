namespace Correto1
{
    public class DebitoContaPoupanca : DebitoConta
    {
        public override string Debitar(decimal valor, string conta)
        {
            // Verifica Aniversario da Conta
            // Debita Conta Poupanca
            return FormatarTransacao();
        }
        
    }
}